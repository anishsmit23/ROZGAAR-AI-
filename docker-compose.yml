version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: job-search-agent
    env_file:
      - .env
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./:/app
    restart: unless-stopped
